fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
alembic>=1.13.1
pydantic>=2.9.0
pydantic-settings>=2.2.1
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
pytest>=8.2.0
httpx>=0.27.0
APScheduler>=3.10.4
python-telegram-bot>=21.0

# KHE_AI extraction module SDKs (anthropic, google-genai).
# The module owns the version pins in its own requirements file; we just
# include it so a single `pip install -r backend/requirements.txt` (and the
# staging/prod deploy that does exactly that) installs the SDKs the worker
# needs at provider-instantiation time. Without this line the factory's
# lazy imports raise → ExtractionUnavailable → status=failed (#79 root cause).
-r modules/extraction/requirements.txt
