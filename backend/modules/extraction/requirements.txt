# KHE_AI extraction module deps.
# Core (always needed):
pydantic>=2.0
# Provider SDKs (imported lazily; only needed to actually call a provider):
anthropic>=0.50.0
google-genai>=1.0.0
# DEC-049: pypdf for splitting large PDFs into chunks (DocAI sync API max 15 pages).
pypdf>=4.0.0
